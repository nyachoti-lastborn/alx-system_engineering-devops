#!/bin/bash
current_dir_files=$(ls -la) parent_dir_files=$(ls -la ..) boot_dir_files=$(ls -la /boot) echo "Files in current directory:" echo "$current_dir_files" echo "Files in parent directory:" echo "$parent_dir_files" echo "Files in /boot directory:" echo "$boot_dir_files"
